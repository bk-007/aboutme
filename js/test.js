

var animation = lottie.loadAnimation({
    container: document.getElementById('lottie1'), // Required
    path: 'json\lottie1.json', // Required
    renderer: 'html', // Required
    loop: true, // Optional
    autoplay: true, // Optional
    name: "Welcome", // Name for future reference. Optional.
  })